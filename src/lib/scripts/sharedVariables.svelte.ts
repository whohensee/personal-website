// Where viable, this should be used to manage the
// responsive widths utilizing tailwind, by adding
// {sharedWidths} to the classlist of anything that
// should conform to the width of the site.
const sharedWidths = ' min-w-xs w-xs sm:w-xl md:w-2xl';

const content_box_styling = 'bg-gray-200 rounded-md p-3';

const button_styling = 'w-fit rounded md border-solid border-black border-2 p-1';

export { sharedWidths, content_box_styling, button_styling };
